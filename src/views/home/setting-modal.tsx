import React from "react";

type propTypes = {
  isModalOpen: boolean;
  setIsModalOpen: (newVal: boolean) => void;
};
const SettingModal = ({ isModalOpen, setIsModalOpen }: propTypes) => (
  <div></div>
);
export default SettingModal;
